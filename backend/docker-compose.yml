version: '3.9'

services:
  backend:
    build: .
    container_name: legal-backend
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NITRO_PORT: 3000
    restart: unless-stopped
